# Core — pinned to a modern, CUDA-capable stack.
# Install torch matching your CUDA version, e.g. for CUDA 12.1:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.2
numpy>=1.24
pyyaml>=6.0

# Chemistry / data (needed once you wire in the real corpora; the synthetic
# path in scripts/run_sft.py runs without them).
# rdkit>=2023.9        # SMILES canonicalization, randomization, salt stripping
# pandas>=2.0          # reading the HTE .xlsx sheets
# openpyxl>=3.1        # .xlsx engine for pandas

# Optional experiment tracking
# wandb>=0.16

# Dev
# pytest>=8.0
